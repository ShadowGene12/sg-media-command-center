## 2026-05-31 - Adding ARIA labels to icon-only buttons
**Learning:** The Command Center UI makes heavy use of icon-only buttons (like those in AppHeader and CommandPalette) for aesthetics, but these often miss `aria-label` attributes, affecting screen reader accessibility.
**Action:** Always verify icon-only buttons have descriptive `aria-label` attributes to ensure keyboard and screen reader accessibility.
