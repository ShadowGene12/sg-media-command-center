## 2024-05-24 - Zustand Store Subscriptions\n**Learning:** Application shell components frequently subscribe to the entire Zustand store (e.g., `useCommandStore()`), causing unnecessary re-renders whenever any unrelated state property changes.\n**Action:** Use specific state selectors (e.g., `useCommandStore(state => state.property)`) in shell components to prevent widespread unnecessary re-renders.
